{% if languages %}
  <ul class="nav navbar-nav ml-auto">
    {% for language in languages %}
      <li class="nav-item{% if language.current %} active{% endif %}">
        <a class="nav-link" href="{{ language.url }}">{{ language.label|uppercase }}</a>
      </li>
    {% endfor %}
  </ul>
{% endif %}
