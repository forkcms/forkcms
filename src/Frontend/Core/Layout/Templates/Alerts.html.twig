{% macro alert(type, message, dismissable, id) %}
  <div{% if id %} id="{{ id }}"{% endif %} class="alert alert-{{ type }}" role="alert">
    {% if dismissable %}
      <button type="button" class="close" data-dismiss="alert">×</button>
    {% endif %}
    {{ message|raw }}
  </div>
{% endmacro %}
