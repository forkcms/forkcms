{#
  variables that are available:
  - {{ tags }}: contains an array with all tags that are used on the site, each element contains data about the tag
#}
<section id="tagsIndex" class="tags">
  {% if not tags %}
  <div class="alert alert-info" role="alert">{{ 'msg.TagsNoItems'|trans }}</div>
  {% else %}
  <ul>
    {% for tag in tags %}
    <li>
      <a href="{{ geturlforblock('Tag':'Detail') }}/{{ tag.url }}" rel="tag">
        {{ tag.name }}
      </a>
      <span class="badge hidden-phone">{{ tag.number }}</span>
    </li>
    {% endfor %}
  </ul>
  {% endif %}
</section>
