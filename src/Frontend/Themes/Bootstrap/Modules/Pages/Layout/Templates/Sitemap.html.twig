{% if navigation %}
  <ul>
    {% for page in navigation %}
      <li>
        <a href="{{ page.link }}" title="{{ page.navigationtitle }}"{% if page.nofollow %} rel="nofollow"{% endif %}>{{ page.navigationtitle }}</a>
        {{ page.children }}
      </li>
    {% endfor %}
  </ul>
{% endif %}
