{#
	variables that are available:
	- {{ widgetFaqOwnQuestion }}: true or false depending on if the data can be shown
#}

{% if widgetFaqOwnQuestion %}
  <section id="faqOwnQuestionForm" class="mod">
    <div class="inner">
      <header class="hd">
        <h3 id="{{ 'act.OwnQuestion'|trans }}">{{ 'msg.AskOwnQuestion'|trans|ucfirst }}</h3>
      </header>
      <div class="bd">
        {% if errorSpam %}
          <div class="message error"><p>{{ 'err.OwnQuestionSpam'|trans }}</p></div>{% endif %}
        {% if success %}
          <div class="message success"><p>{{ 'msg.OwnQuestionSuccess'|trans }}</p></div>{% endif %}

        {% if form_own_question %}
        {% form own_question %}
        <p{% if txtNameError %} class="errorArea"{% endif %}>
          <label for="name">{{ 'lbl.YourName'|trans|ucfirst }}<abbr title="{{ 'lbl.RequiredField'|trans }}">*</abbr></label>
          {% form_field_error name %} {% form_field name %}
        </p>
        <p{% if txtEmailError %} class="errorArea"{% endif %}>
          <label for="email">{{ 'lbl.YourEmail'|trans|ucfirst }}
            <abbr title="{{ 'lbl.RequiredField'|trans }}">*</abbr></label>
          {% form_field_error email %} {% form_field email %}
        </p>
        <p class="bigInput{% if txtMessageError %} errorArea{% endif %}">
          <label for="message">{{ 'lbl.YourQuestion'|trans|ucfirst }}
            <abbr title="{{ 'lbl.RequiredField'|trans }}">*</abbr></label>
          {% form_field_error message %} {% form_field message %}
        </p>
        <p>
          <input class="inputSubmit" type="submit" name="send" value="{{ 'lbl.Send'|trans|ucfirst }}" />
        </p>
        {% endform %}
        {% endif %}
      </div>
    </div>
  </section>
{% endif %}
