{#
  Media Slider with BxSlider
  Variables that are available:
  - {{ item }}: Contains full urls to MediaItem item, f.e.: {{ item.large }} or {{ item.large_resolution }}
#}

{% if item %}
  <section class="widgetMediaOneImage" itemscope itemtype="http://schema.org/ImageGallery">
    <figure itemprop="associatedMedia" itemscope itemtype="http://schema.org/ImageObject">
      <img src="{{ item.large_source }}" itemprop="thumbnail" alt="{{ item.mediaItem.title }}" />
    </figure>
  </section>
{% endif %}
