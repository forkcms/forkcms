{# This file is generated by the Spoon2Twig Converter #}

{% extends '/Layout/Templates/base.html.twig' %}

{% block actionbar %}
  <div class="row fork-module-heading">
    <div class="col-md-6">
      <h2>{{ 'lbl.Categories'|trans|capitalize }}</h2>
    </div>
    <div class="col-md-6">

      {% if showFaqAddCategory %}
      <div class="btn-toolbar pull-right">
        <div class="btn-group" role="group">
          <a href="{{ geturl('add_category' ) }}" class="btn btn-default" title="{{ 'lbl.AddCategory'|trans|capitalize }}">
            <span class="fa fa-plus"></span>&nbsp;
            {{ 'lbl.AddCategory'|trans|capitalize }}
          </a>
        </div>
      </div>
      {% endif %}
    </div>
  </div>
{% endblock %}

{% block content %}
  <div class="row fork-module-content">
    <div class="col-md-12">
      {% if dataGrid %}
        {{ dataGrid|raw }}
      {% endif %}
      {% if not dataGrid %}
        <p>{{ msg.NoCategories }}</p>
      {% endif %}
    </div>
  </div>
{% endblock %}
