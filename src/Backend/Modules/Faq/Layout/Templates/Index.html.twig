{# This file is generated by the Spoon2Twig Converter #}

{% extends '/Layout/Templates/base.html.twig' %}

{% block actionbar %}
    <div class="btn-toolbar pull-right">
      <div class="btn-group" role="group">
        {% if showFaqAdd %}
          {{ macro.buttonIcon( geturl('add'), 'plus', 'lbl.Add'|trans|capitalize) }}
        {% endif %}
      </div>
    </div>
{% endblock %}

{% block content %}
  <div class="row fork-module-content">
    <div class="col-md-12">
      {% if dataGrids %}
        {% for dataGrid in dataGrids %}
          <div id="dataGrid-{{ dataGrid.id }}" class="content-block jsDataGridQuestionsHolder">
            <div class="content-title">
              <p>{{ dataGrid.title }}</p>
            </div>
            {% if dataGrid.content %}
              {{ dataGrid.content | raw }}
            {% endif %}
            {% if not dataGrid.content %}
              {{ emptyDatagrid | raw }}
            {% endif %}
          </div>
        {% endfor %}
      {% endif %}
      {% if not dataGrids %}
        <p>{{ 'msg.NoItems'|trans }}</p>
      {% endif %}
    </div>
  </div>
{% endblock %}
