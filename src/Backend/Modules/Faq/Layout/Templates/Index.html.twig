{# This file is generated by the Spoon2Twig Converter #}

{% include "/Layout/Templates/Head.html.twig" %}
{% include "/Layout/Templates/StructureStartModule.html.twig" %}
<div class="row fork-module-heading">
  <div class="col-md-12">
    <h2>{{ 'lbl.Questions'|trans|capitalize }}</h2>
    <div class="btn-toolbar pull-right">
      <div class="btn-group" role="group">
        {% if showFaqAdd %}
        <a href="{{ geturl('add' ) }}" class="btn btn-default" title="{{ 'lbl.Add'|trans|capitalize }}">
          <span class="glyphicon glyphicon-plus"></span>&nbsp;
          {{ 'lbl.Add'|trans|capitalize }}
        </a>
        {% endif %}
      </div>
    </div>
  </div>
</div>
<div class="row fork-module-content">
  <div class="col-md-12">
    {% if dataGrids %}
    {% for dataGrid in dataGrids %}
    <div id="dataGrid-{{ dataGrid.id }}" class="panel panel-default jsDataGridQuestionsHolder">
      <div class="panel-heading">
        <h3 class="panel-title">{{ dataGrid.title }}</h3>
      </div>
      {% if dataGrid.content %}
      {{ dataGrid.content }}
      {% endif %}
      {% if not dataGrid.content %}
      <div class="panel-body">
        {{ emptyDatagrid }}
      </div>
      {% endif %}
    </div>
    {% endfor %}
    {% endif %}
    {% if not dataGrids %}
    <div class="panel-body">
      <p>{{ msg.NoItems }}</p>
    </div>
    {% endif %}
  </div>
</div>
{% include "/Layout/Templates/StructureEndModule.html.twig" %}
{% include "/Layout/Templates/Footer.html.twig" %}
