{# This file is generated by the Spoon2Twig Converter #}

{% extends '/Layout/Templates/base.html.twig' %}

{% block content %}
  {% form themes %}
  <div class="row fork-module-heading">
    <div class="col-md-12">
      <h2 class="form-inline">{{ 'lbl.Templates'|trans|capitalize }} {{ lbl.For }} {% form_field theme %}</h2>
      {% if showExtensionsAddThemeTemplate %}
      <div class="btn-toolbar pull-right">
        <div class="btn-group" role="group">
          <a href="{{ geturl('add_theme_template' ) }}{% if selectedTheme %}&amp;theme={{ selectedTheme }}{% endif %}" class="btn btn-primary">
            <span class="fa fa-plus"></span>&nbsp;
            <span>{{ 'lbl.AddTemplate'|trans|capitalize }}</span>
          </a>
          <a href="{{ geturl('export_theme_templates' ) }}{% if selectedTheme %}&amp;theme={{ selectedTheme }}{% endif %}" class="btn btn-default">
            <span class="fa fa-download"></span>&nbsp;
            <span>{{ 'lbl.Export'|trans|capitalize }}</span>
          </a>
        </div>
      </div>
      {% endif %}
    </div>
  </div>
  <div class="row fork-module-content">
    <div class="col-md-12">
      {% if dataGrid %}
      {{ dataGrid|raw }}
      {% endif %}
      {% if not dataGrid %}
      <p>{{ msg.NoItems }}</p>
      {% endif %}
    </div>
  </div>
  {% endform %}
{% endblock %}
