{# This file is generated by the Spoon2Twig Converter #}

{% extends '/Layout/Templates/base.html.twig' %}

{% block content %}
  <div class="row fork-module-heading">
    <div class="col-md-6">
      <h2>{{ 'lbl.Synonyms'|trans|capitalize }}</h2>
    </div>
    <div class="col-md-6">

      {% if showSearchAddSynonym %}
      <div class="btn-toolbar pull-right">
        <div class="btn-group" role="group">
          <a href="{{ geturl('add_synonym' ) }}" class="btn btn-default" title="{{ 'lbl.AddSynonym'|trans|capitalize }}">
            <span class="fa fa-plus"></span>
            {{ 'lbl.AddSynonym'|trans|capitalize }}
          </a>
        </div>
      </div>
      {% endif %}
    </div>
  </div>
  <div class="row fork-module-content">
    <div class="col-md-12">
      {% if dataGrid %}
      {{ dataGrid }}
      {% endif %}
      {% if not dataGrid %}
      <p>{{ 'msg.NoSynonyms'|trans|sprintf(geturl('add_synonym' ) ) }}</p>
      {% endif %}
    </div>
  </div>
{% endblock %}
