{# This file is generated by the Spoon2Twig Converter #}

{% include "/Layout/Templates/Head.html.twig" %}
{% include "/Layout/Templates/StructureStartModule.html.twig" %}
<div class="row fork-module-heading">
  <div class="col-md-12">
    <h2>{{ 'lbl.Comments'|trans|capitalize }}</h2>
  </div>
</div>
<div class="row fork-module-content">
  <div class="col-md-12">
    <div role="tabpanel">
      <ul class="nav nav-tabs" role="tablist">
        <li role="presentation" class="active">
          <a href="#tabPublished" aria-controls="published" role="tab" data-toggle="tab">{{ 'lbl.Published'|trans|capitalize }}</a>
        </li>
        <li role="presentation">
          <a href="#tabModeration" aria-controls="moderation" role="tab" data-toggle="tab">{{ 'lbl.WaitingForModeration'|trans|capitalize }}</a>
        </li>
        <li role="presentation">
          <a href="#tabSpam" aria-controls="spam" role="tab" data-toggle="tab">{{ 'lbl.Spam'|trans|capitalize }}</a>
        </li>
      </ul>
      <div class="tab-content">
        <div role="tabpanel" class="tab-pane active" id="tabPublished">
          <div class="row">
            <div class="col-md-12">
              <h3>{{ 'lbl.Published'|trans|capitalize }}</h3>
            </div>
          </div>
          {% if dgPublished %}
          <form act.on="{{  geturl('mass_comment_action') }}" method="get" class="forkForms" id="commentsPublished">
            <input type="hidden" name="from" value="published" />
            {{ dgPublished|raw }}
            <div class="modal fade" id="confirmPublishedToSpam" tabindex="-1" role="dialog" aria-labelledby="{{ 'lbl.Spam'|trans|capitalize }}" aria-hidden="true">
              <div class="modal-dialog">
                <div class="modal-content">
                  <div class="modal-header">
                    <span class="modal-title h4">{{ 'lbl.Spam'|trans|capitalize }}</span>
                  </div>
                  <div class="modal-body">
                    <p>{{ 'msg.ConfirmMassSpam'|trans|capitalize }}</p>
                  </div>
                  <div class="modal-footer">
                    <button type="button" class="btn btn-default" data-dismiss="modal">{{ 'lbl.Cancel'|trans|capitalize }}</button>
                    <button type="submit" class="btn btn-primary">{{ 'lbl.OK'|trans|capitalize }}</button>
                  </div>
                </div>
              </div>
            </div>
            <div class="modal fade" id="confirmDeletePublished" tabindex="-1" role="dialog" aria-labelledby="{{ 'lbl.Delete'|trans|capitalize }}" aria-hidden="true">
              <div class="modal-dialog">
                <div class="modal-content">
                  <div class="modal-header">
                    <span class="modal-title h4">{{ 'lbl.Delete'|trans|capitalize }}</span>
                  </div>
                  <div class="modal-body">
                    <p>{{ 'msg.ConfirmMassDelete'|trans|capitalize }}</p>
                  </div>
                  <div class="modal-footer">
                    <button type="button" class="btn btn-default" data-dismiss="modal">{{ 'lbl.Cancel'|trans|capitalize }}</button>
                    <button type="submit" class="btn btn-primary">{{ 'lbl.OK'|trans|capitalize }}</button>
                  </div>
                </div>
              </div>
            </div>
          </form>
          {% endif %}
          {% if not dgPublished %}
          <p>{{ 'msg.NoComments'|trans|capitalize }}</p>
          {% endif %}
        </div>
        <div role="tabpanel" class="tab-pane" id="tabModeration">
          <div class="row">
            <div class="col-md-12">
              <h3>{{ 'lbl.WaitingForModeration'|trans|capitalize }}</h3>
            </div>
          </div>
          {% if dgModeration %}
          <form act.on="{{  geturl('mass_comment_action') }}" method="get" class="forkForms" id="commentsModeration">
            <input type="hidden" name="from" value="moderation" />
            {{ dgModeration|raw }}
            <div class="modal fade" id="confirmModerationToSpam" tabindex="-1" role="dialog" aria-labelledby="{{ 'lbl.Spam'|trans|capitalize }}" aria-hidden="true">
              <div class="modal-dialog">
                <div class="modal-content">
                  <div class="modal-header">
                    <span class="modal-title h4">{{ 'lbl.Spam'|trans|capitalize }}</span>
                  </div>
                  <div class="modal-body">
                    <p>{{ 'msg.ConfirmMassSpam'|trans|capitalize }}</p>
                  </div>
                  <div class="modal-footer">
                    <button type="button" class="btn btn-default" data-dismiss="modal">{{ 'lbl.Cancel'|trans|capitalize }}</button>
                    <button type="submit" class="btn btn-primary">{{ 'lbl.OK'|trans|capitalize }}</button>
                  </div>
                </div>
              </div>
            </div>
            <div class="modal fade" id="confirmDeleteModeration" tabindex="-1" role="dialog" aria-labelledby="{{ 'lbl.Delete'|trans|capitalize }}" aria-hidden="true">
              <div class="modal-dialog">
                <div class="modal-content">
                  <div class="modal-header">
                    <span class="modal-title h4">{{ 'lbl.Delete'|trans|capitalize }}</span>
                  </div>
                  <div class="modal-body">
                    <p>{{ 'msg.ConfirmMassDelete'|trans|capitalize }}</p>
                  </div>
                  <div class="modal-footer">
                    <button type="button" class="btn btn-default" data-dismiss="modal">{{ 'lbl.Cancel'|trans|capitalize }}</button>
                    <button type="submit" class="btn btn-primary">{{ 'lbl.OK'|trans|capitalize }}</button>
                  </div>
                </div>
              </div>
            </div>
          </form>
          {% endif %}
          {% if not dgModeration %}
          <p>{{ 'msg.NoComments'|trans|capitalize }}</p>
          {% endif %}
        </div>
        <div role="tabpanel" class="tab-pane" id="tabSpam">
          <div class="row">
            <div class="col-md-12">
              <h3>{{ 'lbl.Spam'|trans|capitalize }}</h3>
            </div>
          </div>
          {% if dgSpam %}
          <form act.on="{{  geturl('mass_comment_action') }}" method="get" class="forkForms" id="commentsSpam">
            <input type="hidden" name="from" value="spam" />
            <div class="alert alert-info">
              {{ 'msg.DeleteAllSpam'|trans|capitalize }}
              <a href="{{ geturl('delete_spam' ) }}">{{ 'lbl.Delete'|trans|capitalize }}</a>
            </div>
            {{ dgSpam|raw }}
            <div class="modal fade" id="confirmDeleteSpam" tabindex="-1" role="dialog" aria-labelledby="{{ 'lbl.Delete'|trans|capitalize }}" aria-hidden="true">
              <div class="modal-dialog">
                <div class="modal-content">
                  <div class="modal-header">
                    <span class="modal-title h4">{{ 'lbl.Delete'|trans|capitalize }}</span>
                  </div>
                  <div class="modal-body">
                    <p>{{ 'msg.ConfirmMassDelete'|trans|capitalize }}</p>
                  </div>
                  <div class="modal-footer">
                    <button type="button" class="btn btn-default" data-dismiss="modal">{{ 'lbl.Cancel'|trans|capitalize }}</button>
                    <button type="submit" class="btn btn-primary">{{ 'lbl.OK'|trans|capitalize }}</button>
                  </div>
                </div>
              </div>
            </div>
          </form>
          {% endif %}
          {% if not dgSpam %}
          <p>{{ 'msg.NoComments'|trans|capitalize }}</p>
          {% endif %}
        </div>
      </div>
    </div>
  </div>
</div>
{% include "/Layout/Templates/StructureEndModule.html.twig" %}
{% include "/Layout/Templates/Footer.html.twig" %}
