{# This file is generated by the Spoon2Twig Converter #}

{% extends '/Layout/Templates/base.html.twig' %}

{% block content %}
  <div class="row fork-module-heading">
    <div class="col-md-6">
      <h2>{{ 'lbl.Translations'|trans|capitalize }}</h2>
    </div>
    <div class="col-md-6">

      {% if showLocaleExportAnalyse %}
      <div class="btn-toolbar pull-right">
        <div class="btn-group" role="group">
          <a href="{{ geturl('ExportAnalyse' ) }}&amp;language={{ language }}" class="btn btn-default" title="{{ 'lbl.Export'|trans|capitalize }}">
            <span class="fa fa-plus"></span>
            {{ 'lbl.Export'|trans|capitalize }}
          </a>
        </div>
      </div>
      {% endif %}
    </div>
  </div>
  <div class="row fork-module-content">
    <div class="col-md-12">
      {% if dgFrontend %}
      <h4>{{ 'lbl.Frontend'|trans|capitalize }}</h4>
      {{ dgFrontend }}
      {% endif %}
      {% if not dgFrontend %}
      <h4>{{ 'lbl.Frontend'|trans|capitalize }}</h4>
      <p>{{ msg.NoItemsAnalyse }}</p>
      {% endif %}
      {% if dgBackend %}
      <h4>{{ 'lbl.Backend'|trans|capitalize }}</h4>
      {{ dgBackend }}
      {% endif %}
      {% if not dgBackend %}
      <h4>{{ 'lbl.Backend'|trans|capitalize }}</h4>
      <p>{{ msg.NoItemsAnalyse }}</p>
      {% endif %}
    </div>
  </div>
{% endblock %}
