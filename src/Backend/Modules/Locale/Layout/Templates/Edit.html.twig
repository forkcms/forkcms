{# This file is generated by the Spoon2Twig Converter #}

{% extends '/Layout/Templates/base.html.twig' %}

{% block content %}
  <div class="row fork-module-heading">
    <div class="col-md-6">
      <h2>{{ 'msg.EditTranslation'|trans|capitalize|sprintf(name ) }}</h2>
    </div>
    <div class="col-md-6">

    </div>
  </div>
  {% form edit %}
    <div class="row fork-module-content">
      <div class="col-md-12">
        <div class="form-group">
          <label for="name" class="control-label">
            {{ 'lbl.ReferenceCode'|trans|capitalize }}
            <abbr data-toggle="tooltip" title="{{ 'lbl.RequiredField'|trans|capitalize }}">*</abbr>
          </label>
          <p class="help-block">{{ msg.HelpAddName }}</p>
          {% form_field_error name %} {% form_field name %}
        </div>
        <div class="form-group">
          <label for="value" class="control-label">
            {{ 'lbl.Translation'|trans|capitalize }}
            <abbr data-toggle="tooltip" title="{{ 'lbl.RequiredField'|trans|capitalize }}">*</abbr>
          </label>
          <p class="help-block">{{ msg.HelpAddValue }}</p>
          {% form_field_error value %} {% form_field value %}
        </div>
        <div class="form-group">
          <label for="language" class="control-label">{{ 'lbl.Language'|trans|capitalize }}</label>
          {% form_field_error language %} {% form_field language %}
        </div>
        <div class="form-group">
          <label for="application" class="control-label">{{ 'lbl.Application'|trans|capitalize }}</label>
          {% form_field_error application %} {% form_field application %}
        </div>
        <div class="form-group">
          <label for="module" class="control-label">{{ 'lbl.Module'|trans|capitalize }}</label>
          {% form_field_error module %} {% form_field module %}
        </div>
        <div class="form-group">
          <label for="type" class="control-label">{{ 'lbl.Type'|trans|capitalize }}</label>
          {% form_field_error type %} {% form_field type %}
        </div>
      </div>
    </div>
    <div class="row fork-module-actions">
      <div class="col-md-12">
        <div class="btn-toolbar">
          <div class="btn-group pull-right" role="group">
            <button id="editButton" type="submit" name="edit" class="btn btn-primary">{{ 'lbl.Save'|trans|capitalize }}</button>
          </div>
        </div>
      </div>
    </div>
  {% endform %}
<<<<<<< Updated upstream
{% extends '/Layout/Templates/base.html.twig' %}

{% block content %}
=======
{% endblock %}
>>>>>>> Stashed changes
