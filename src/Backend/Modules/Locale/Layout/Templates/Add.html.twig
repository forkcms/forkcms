{# This file is generated by the Spoon2Twig Converter #}

{% include "/Layout/Templates/Head.html.twig" %}
{% include "/Layout/Templates/StructureStartModule.html.twig" %}
<div class="row fork-module-actions">
  <div class="col-md-12">
    <h2>{{ 'lbl.Add'|trans|capitalize }}</h2>
  </div>
</div>
{% form add %}
  <div class="row fork-module-content">
    <div class="col-md-12">
      <div class="form-group">
        <label for="name">
          {{ 'lbl.ReferenceCode'|trans|capitalize }}
          <abbr class="glyphicon glyphicon-asterisk" title="{{ 'lbl.RequiredField'|trans|capitalize }}"></abbr>
        </label>
        <p class="text-info">{{ msg.HelpAddName }}</p>
        {% form_field_error name %} {% form_field name %}
      </div>
      <div class="form-group">
        <label for="value">
          {{ 'lbl.Translation'|trans|capitalize }}
          <abbr class="glyphicon glyphicon-asterisk" title="{{ 'lbl.RequiredField'|trans|capitalize }}"></abbr>
        </label>
        <p class="text-info">{{ msg.HelpAddValue }}</p>
        {% form_field_error value %} {% form_field value %}
      </div>
      <div class="form-group">
        <label for="language">{{ 'lbl.Language'|trans|capitalize }}</label>
        {% form_field_error language %} {% form_field language %}
      </div>
      <div class="form-group">
        <label for="application">{{ 'lbl.Application'|trans|capitalize }}</label>
        {% form_field_error application %} {% form_field application %}
      </div>
      <div class="form-group">
        <label for="module">{{ 'lbl.Module'|trans|capitalize }}</label>
        {% form_field_error module %} {% form_field module %}
      </div>
      <div class="form-group">
        <label for="type">{{ 'lbl.Type'|trans|capitalize }}</label>
        {% form_field_error type %} {% form_field type %}
      </div>
    </div>
  </div>
  <div class="row fork-module-actions">
    <div class="col-md-12">
      <div class="btn-toolbar">
        <div class="btn-group pull-right" role="group">
          <button id="addButton" type="submit" name="add" class="btn btn-primary">
            <span class="glyphicon glyphicon-plus"></span>&nbsp;
            {{ 'lbl.Add'|trans|capitalize }}
          </button>
        </div>
      </div>
    </div>
  </div>
{% endform %}
{% include "/Layout/Templates/StructureEndModule.html.twig" %}
{% include "/Layout/Templates/Footer.html.twig" %}
