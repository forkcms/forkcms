{# This file is generated by the Spoon2Twig Converter #}

{% extends '/Layout/Templates/base.html.twig' %}

{% block actionbar %}
    <div class="btn-toolbar pull-right">
      <div class="btn-group" role="group">
        {% if showLocaleAdd %}
          {{ macro.buttonIcon(geturl('add')  ~ filter, 'plus', 'lbl.Add'|trans|capitalize, 'btn-default jsButtonAdd' ) }}
        {% endif %}
        {% if showLocaleExport %}
          {{ macro.buttonIcon(geturl('export' ) ~ filter, 'download', 'lbl.Export'|trans|capitalize, 'btn-default jsButtonExport' ) }}
        {% endif %}
        {% if showLocaleImport %}
          {{ macro.buttonIcon(geturl('import' ) ~ filter, 'upload', 'lbl.Import'|trans|capitalize, 'btn-default jsButtonImport') }}
        {% endif %}
      </div>
    </div>
{% endblock %}

{% block content %}
  <div class="row fork-module-content">
    <div class="col-md-12">
      {% form filter %}
        <div class="panel panel-default">
          <div class="panel-body">
            <div class="row">
              <div class="col-md-3">
                <div class="form-group">
                  <label for="application" class="control-label">{{ 'lbl.Application'|trans|capitalize }}</label>
                  {% form_field_error application %} {% form_field application %}
                </div>
                <div class="form-group">
                  <label for="module" class="control-label">{{ 'lbl.Module'|trans|capitalize }}</label>
                  {% form_field_error module %} {% form_field module %}
                </div>
              </div>
              <div class="col-md-3">
                <div class="form-group">
                  <label>{{ 'lbl.Types'|trans|capitalize }}</label>
                  {% if type %}
                    <ul class="list-unstyled">
                      {% for item in type %}
                        <li class="checkbox">
                          <label for="{{ item.id }}">{{ item.chkType | raw }} {{ item.label|capitalize }}</label>
                        </li>
                      {% endfor %}
                    </ul>
                  {% endif %}
                </div>
              </div>
              <div class="col-md-3">
                <div class="form-group">
                  <label>{{ 'lbl.Languages'|trans|capitalize }}</label>
                  {% if language %}
                    <ul class="list-unstyled">
                      {% for item in language %}
                        <li class="checkbox">
                          <label for="{{ item.id }}">{{ item.chkLanguage | raw }} {{ item.label|capitalize }}</label>
                        </li>
                      {% endfor %}
                    </ul>
                  {% endif %}
                </div>
              </div>
              <div class="col-md-3">
                <div class="form-group">
                  <label for="name" class="control-label">
                    {{ 'lbl.ReferenceCode'|trans|capitalize }}&nbsp;
                    <abbr class="fa fa-info-circle" data-toggle="tooltip" title="{{ 'msg.HelpName'|trans }}"></abbr>
                  </label>
                  {% form_field_error name %} {% form_field name %}
                </div>
                <div class="form-group">
                  <label for="value" class="control-label">
                    {{ 'lbl.Value'|trans|capitalize }}&nbsp;
                    <abbr class="fa fa-info-circle" data-toggle="tooltip" title="{{ 'msg.HelpValue'|trans }}"></abbr>
                  </label>
                  {% form_field_error value %} {% form_field value %}
                </div>
              </div>
            </div>
          </div>
          <div class="panel-footer">
            <div class="btn-toolbar">
              <div class="btn-group pull-right">
                <button id="search" class="btn btn-primary" type="submit" name="search">
                  <span class="fa fa-refresh"></span>&nbsp;
                  {{ 'lbl.UpdateFilter'|trans|capitalize }}
                </button>
              </div>
            </div>
          </div>
        </div>
      {% endform %}
    </div>
  </div>
  {% if dgLabels %}
  <div class="row fork-module-content">
    <div class="col-md-12">
      <div class="panel panel-default">
        <div class="panel-heading">
          <h3 class="panel-title">
            {{ 'lbl.Labels'|trans|capitalize }}
          </h3>
        </div>
        {{ dgLabels | raw}}
      </div>
    </div>
  </div>
  {% endif %}
  {% if dgMessages %}
  <div class="row fork-module-content">
    <div class="col-md-12">
      <div class="panel panel-default">
        <div class="panel-heading">
          <h3 class="panel-title">
            {{ 'lbl.Messages'|trans|capitalize }}
          </h3>
        </div>
        {{ dgMessages | raw}}
      </div>
    </div>
  </div>
  {% endif %}
  {% if dgErrors %}
  <div class="row fork-module-content">
    <div class="col-md-12">
      <div class="panel panel-default">
        <div class="panel-heading">
          <h3 class="panel-title">
            {{ 'lbl.errors'|trans|capitalize }}
          </h3>
        </div>
        {{ dgErrors | raw }}
      </div>
    </div>
  </div>
  {% endif %}
  {% if dgActions %}
  <div class="row fork-module-content">
    <div class="col-md-12">
      <div class="panel panel-default">
        <div class="panel-heading">
          <h3 class="panel-title">
            {{ 'lbl.actions'|trans|capitalize }}&nbsp;
            <abbr class="fa fa-info-circle" data-toggle="tooltip" title="{{ 'msg.HelpActionValue'|trans }}"></abbr>
          </h3>
        </div>
        {{ dgActions | raw }}
      </div>
    </div>
  </div>
  {% endif %}
  {% if noItems %}
  <div class="row fork-module-content">
    <div class="col-md-12">
      <div class="panel panel-default">
        <div class="panel-body">
          <p>{{ 'msg.NoItemsFilter'|trans|sprintf(addURL ) }}</p>
        </div>
      </div>
    </div>
  </div>
  {% endif %}
{% endblock %}
