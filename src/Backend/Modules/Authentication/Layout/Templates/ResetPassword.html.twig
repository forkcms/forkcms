{# This file is generated by the Spoon2Twig Converter #}

{% include "/Layout/Templates/Head.html.twig" %}
<body id="login">
  {% include "/{{ MODULE }}/Layout/Templates/Ie6.html.twig" %}
  <div class="page-header text-center">
    <h1>{{ SITE_TITLE }}</h1>
  </div>
  <div class="container">
    <div class="row">
      <div class="col-md-6 col-md-offset-3">
        {% if debug %}
        <div class="alert alert-warning">
          <p>{{ msg.WarningDebugMode }}</p>
        </div>
        {% endif %}
        {% form authenticationResetPassword %}
          <div class="panel panel-default">
            <div class="panel-body">
              <p class="help-block">{{ msg.HelpResetPassword }}</p>
              <div class="form-group">
                <label for="backendNewPassword" class="control-label">{{ 'lbl.NewPassword'|trans|capitalize }}</label>
                {% form_field_error backend_new_password %} {% form_field backend_new_password %}
              </div>
              <div class="form-group">
                <label for="backendNewPasswordRepeated" class="control-label">{{ 'lbl.RepeatPassword'|trans|capitalize }}</label>
                {% form_field_error backend_new_password_repeated %} {% form_field backend_new_password_repeated %}
              </div>
              <div class="form-group">
                <div class="btn-toolbar pull-right">
                  <div class="btn-group">
                    <button id="resetPassword" name="reset" type="submit" class="btn btn-primary">
                      {{ 'lbl.ResetAndSignIn'|trans|capitalize }}
                    </button>
                  </div>
                </div>
              </div>
            </div>
          </div>
        {% endform %}
      </div>
    </div>
  </div>
{% include "/Layout/Templates/Footer.html.twig" %}
