{# This file is generated by the Spoon2Twig Converter #}

{% if visitors_graph_data %}
  <div class="box" id="widgetAnalyticsRecentVisits">
    <div class="heading">
      <h3>{{ 'lbl.RecentVisits'|trans|capitalize }}</h3>
    </div>
    <div class="options">
      <div id="dataChartDoubleMetricPerDay" class="hidden">
        <span id="maxYAxis">{{ maxYAxis }}</span>
        <span id="tickInterval">{{ tickInterval }}</span>
        <span id="yAxisTitle">{{ 'lbl.Visits'|trans|capitalize }}</span>
        <ul class="series">
          <li class="serie" id="metric1serie">
            <span class="name">Pageviews</span>
            <ul class="data">
              {% for _itr_2 in visitors_graph_data %}
              <li>
                <span class="fulldate">{{ _itr_2.ga_date|spoondate('D d M',INTERFACE_LANGUAGE|capitalize ) }}</span>
                <span class="date">{{ _itr_2.ga_date|spoondate('d M',INTERFACE_LANGUAGE|capitalize ) }}</span>
                <span class="value">{{ _itr_2.ga_pageviews }}</span>
              </li>
              {% endfor %}
            </ul>
          </li>
          <li class="serie" id="metric2serie">
            <span class="name">Visitors</span>
            <ul class="data">
              {% for _itr_2 in visitors_graph_data %}
              <li>
                <span class="fulldate">{{ _itr_2.ga_date|spoondate('D d M',INTERFACE_LANGUAGE|capitalize ) }}</span>
                <span class="date">{{ _itr_2.ga_date|spoondate('d M',INTERFACE_LANGUAGE|capitalize ) }}</span>
                <span class="value">{{ _itr_2.ga_users }}</span>
              </li>
              {% endfor %}
            </ul>
          </li>
        </ul>
      </div>
      <div id="chartDoubleMetricPerDay">&nbsp;</div>
    </div>
    <div class="footer">
      <div class="buttonHolderRight">
        <a href="{{ geturl('Index','Analytics' ) }}" class="button"><span>{{ 'lbl.AllStatistics'|trans|capitalize }}</span></a>
      </div>
    </div>
  </div>
{% endif %}
