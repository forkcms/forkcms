services:
    formbuilder.submitted.mailer:
        class: ForkCMS\Frontend\Modules\FormBuilder\EventListener\FormBuilderSubmittedMailSubscriber
        arguments:
            - "@mailer"
            - "@fork.settings"
        tags:
            - { name: kernel.event_listener, event: form.submitted, method: onFormSubmitted }
