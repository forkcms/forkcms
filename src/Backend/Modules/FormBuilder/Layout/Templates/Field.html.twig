{# This file is generated by the Spoon2Twig Converter #}

<div id="fieldHolder-{{ id }}" class="form-group jsField">
  <div class="row">
    {% if plaintext %}
    <div class="col-md-8">
      {{ content }}
    </div>
    {% endif %}
    {% if simple %}
    <div class="col-md-3">
      <label for="field{{ id }}">
        {{ label }}
        {% if required %}
        <abbr data-toggle="tooltip" title="{{ 'lbl.RequiredField'|trans|capitalize }}">*</abbr>
        {% endif %}
      </label>
    </div>
    <div class="col-md-5">
      {{ field|raw }}
    </div>
    {% endif %}
    {% if multiple %}
    <div class="col-md-3">
      <label for="field{{ id }}">
        {{ label }}
        {% if required %}
        <abbr data-toggle="tooltip" title="{{ 'lbl.RequiredField'|trans|capitalize }}">*</abbr>
        {% endif %}
      </label>
    </div>
    <div class="col-md-5">
      <ul class="list-unstyled">
        {% for item in items %}
        <li class="checkbox">
          <label for="{{ item.id }}">{{ item.field }} {{ item.label }}</label>
        </li>
        {% endfor %}
      </ul>
    </div>
    {% endif %}
    <div class="col-md-4">
      <div class="btn-toolbar">
        <div class="btn-group pull-right" role="group">
          <span class="btn dragAndDropHandle">
            <span class="fa fa-reorder"></span>
          </span>
        </div>
        <div class="btn-group pull-right" role="group">
          <a class="btn btn-danger jsFieldDelete" href="#delete-{{ id }}" rel="{{ id }}" title="{{ lbl.Delete }}">
            <span class="fa fa-trash-o"></span>
          </a>
          <a class="btn btn-default jsFieldEdit" href="#edit-{{ id }}" rel="{{ id }}" title="{{ lbl.Edit }}">
            <span class="fa fa-check"></span>
          </a>
        </div>
      </div>
    </div>
  </div>
</div>
