{# This file is generated by the Spoon2Twig Converter #}

{% extends '/Layout/Templates/base.html.twig' %}

{% block actionbar %}

{% endblock %}

{% block content %}
  {% form settingsIndex %}
    {% if warnings %}
    <div class="row fork-module-messages">
      <div class="col-md-12">
        <div class="alert alert-warning" role="alert">
          <p><strong>{{ 'msg.ConfigurationError'|trans|capitalize }}</strong></p>
          <ul>
            {% for warning in warnings %}
            <li>{{ warning.message|raw }}</li>
            {% endfor %}
          </ul>
        </div>
      </div>
    </div>
    {% endif %}
    <div class="row fork-module-content">
      <div class="col-md-12">
        <div class="panel panel-default">
          <div class="panel-heading">
            <h3 class="panel-title">
              {{ 'lbl.WebsiteTitle'|trans|capitalize }}
              <abbr data-toggle="tooltip" title="{{ 'lbl.RequiredField'|trans|capitalize }}">*</abbr>
            </h3>
          </div>
          <div class="panel-body">
            <div class="form-group">
              {% form_field_error site_title %} {% form_field site_title %}
            </div>
          </div>
        </div>
      </div>
    </div>
    <div class="row fork-module-content">
      <div class="col-md-12">
        <div class="panel panel-default">
          <div class="panel-heading">
            <h3 class="panel-title">{{ 'lbl.Scripts'|trans|capitalize }}</h3>
          </div>
          <div class="panel-body">
            <div class="form-group">
              <label for="siteHtmlHeader" class="control-label"><code>&lt;head&gt;</code> script(s)</label>
              {% form_field_error site_html_header %} {% form_field site_html_header %}
              <span class="help-block">{{ msg.HelpScriptsHead }}</span>
            </div>
          </div>
          <div class="panel-body">
            <div class="form-group">
              <label for="siteHtmlFooter" class="control-label">End of <code>&lt;body&gt;</code> script(s)</label>
              {% form_field_error site_html_footer %} {% form_field site_html_footer %}
              <span class="help-block">{{ msg.HelpScriptsFoot }}</span>
            </div>
          </div>
        </div>
      </div>
    </div>
    <div class="row fork-module-content">
      <div class="col-md-12">
        <div class="panel panel-default">
          <div class="panel-heading">
            <h3 class="panel-title">{{ 'lbl.Languages'|trans|capitalize }}</h3>
          </div>
          <div class="panel-body">
            <div class="form-group">
              <p>{{ 'msg.HelpLanguages'|trans|capitalize }}</p>
              <ul id="activeLanguages" class="list-unstyled">
                {% for activeLanguage in activeLanguages %}
                <li class="checkbox">
                  <label for="{{ activeLanguage.id }}">
                    {{ activeLanguage.chkActiveLanguages|raw }} {{ activeLanguage.label }}{% if activeLanguage.default %} ({{ 'lbl.Default'|trans|capitalize }}){% endif %}
                  </label>
                </li>
                {% endfor %}
              </ul>
            </div>
          </div>
          <div class="panel-body">
            <div class="form-group">
              <p>{{ 'msg.HelpRedirectLanguages'|trans|capitalize }}</p>
              <ul id="redirectLanguages" class="list-unstyled">
                {% for redirectLanguage in redirectLanguages %}
                <li class="checkbox">
                  <label for="{{ redirectLanguage.id }}">
                    {{ redirectLanguage.chkRedirectLanguages|raw }} {{ redirectLanguage.label }}{% if redirectLanguage.default %} ({{ 'lbl.Default'|trans|capitalize }}){% endif %}
                  </label>
                </li>
                {% endfor %}
              </ul>
            </div>
          </div>
        </div>
      </div>
    </div>
    <div class="row fork-module-content">
      <div class="col-md-12">
        <div class="panel panel-default">
          <div class="panel-heading">
            <h3 class="panel-title">{{ 'lbl.DateAndTime'|trans|capitalize }}</h3>
          </div>
          <div class="panel-body">
            <div class="form-group">
              <label for="timeFormat" class="control-label">{{ 'lbl.TimeFormat'|trans|capitalize }}</label>
              {% form_field_error time_format %} {% form_field time_format %}
              <span class="help-block">{{ 'msg.HelpTimeFormat'|trans|capitalize }}</span>
            </div>
            <div class="form-group">
              <label for="dateFormatShort" class="control-label">{{ 'lbl.ShortDateFormat'|trans|capitalize }}</label>
              {% form_field_error date_format_short %} {% form_field date_format_short %}
              <span class="help-block">{{ 'msg.HelpDateFormatShort'|trans|capitalize }}</span>
            </div>
            <div class="form-group">
              <label for="dateFormatLong" class="control-label">{{ 'lbl.LongDateFormat'|trans|capitalize }}</label>
              {% form_field_error date_format_long %} {% form_field date_format_long %}
              <span class="help-block">{{ 'msg.HelpDateFormatLong'|trans|capitalize }}</span>
            </div>
          </div>
        </div>
      </div>
    </div>
    <div class="row fork-module-content">
      <div class="col-md-12">
        <div class="panel panel-default">
          <div class="panel-heading">
            <h3 class="panel-title">{{ 'lbl.Numbers'|trans|capitalize }}</h3>
          </div>
          <div class="panel-body">
            <div class="form-group">
              <label for="numberFormat" class="control-label">{{ 'lbl.NumberFormat'|trans|capitalize }}</label>
              {% form_field_error number_format %} {% form_field number_format %}
              <span class="help-block">{{ 'msg.HelpNumberFormat'|trans|capitalize }}</span>
            </div>
          </div>
        </div>
      </div>
    </div>
    <div class="row fork-module-content">
      <div class="col-md-12">
        <div id="settingsApiKeys" class="panel panel-default">
          <div class="panel-heading">
            <h3 class="panel-title">{{ 'lbl.APIKeys'|trans|capitalize }}</h3>
          </div>
          <div class="panel-body">
            <p>{{ msg.HelpAPIKeys }}</p>
            <table class="table table-striped">
              <thead>
                <tr>
                  <th class="title" style="width: 20%;"><span>{{ 'lbl.Name'|trans|capitalize }}</span></th>
                  <th style="width: 40%;"><span>{{ 'lbl.APIKey'|trans|capitalize }}</span></th>
                  <th style="width: 60%;"><span>{{ 'lbl.APIURL'|trans|capitalize }}</span></th>
                </tr>
              </thead>
              <tbody>
                <tr>
                  <th><label for="forkApiPublicKey" class="control-label">Fork public key</label></td>
                  <td>{% form_field_error fork_api_public_key %} {% form_field fork_api_public_key %}</td>
                  <td>&nbsp;</td>
                </tr>
                <tr>
                  <th><label for="forkApiPrivateKey" class="control-label">Fork private key</label></td>
                  <td>{% form_field_error fork_api_private_key %} {% form_field fork_api_private_key %}</td>
                  <td>&nbsp;</td>
                </tr>
                {% if needsGoogleMaps %}
                <tr>
                  <th>
                    <label for="googleMapsKey" class="control-label">
                      Google maps key<abbr data-toggle="tooltip" title="{{ 'lbl.RequiredField'|trans|capitalize }}">*</abbr>
                    </label>
                  </td>
                  <td>{% form_field_error google_maps_key %} {% form_field google_maps_key %}</td>
                  <td><a href="http://code.google.com/apis/maps/signup.html">http://code.google.com/apis/maps/signup.html</a></td>
                </tr>
                {% endif %}
                {% if needsAkismet %}
                <tr>
                  <th><label for="akismetKey" class="control-label">Akismet key</label></td>
                  <td>{% form_field_error akismet_key %} {% form_field akismet_key %}</td>
                  <td><a href="http://akismet.com/personal">http://akismet.com/personal</a></td>
                </tr>
                {% endif %}
              </tbody>
            </table>
          </div>
        </div>
      </div>
    </div>
    <div class="row fork-module-content">
      <div class="col-md-12">
        <div class="panel panel-default">
          <div class="panel-heading">
            <h3 class="panel-title">CKFinder</h3>
          </div>
          <div class="panel-body">
            <div class="form-group">
              <label for="ckfinderLicenseName" class="control-label">{{ 'lbl.LicenseName'|trans|capitalize }}</label>
              {% form_field_error ckfinder_license_name %} {% form_field ckfinder_license_name %}
            </div>
            <div class="form-group">
              <label for="ckfinderLicenseKey" class="control-label">{{ 'lbl.LicenseKey'|trans|capitalize }}</label>
              {% form_field_error ckfinder_license_key %} {% form_field ckfinder_license_key %}
            </div>
            <div class="form-group">
              <label for="ckfinderImageMaxWidth" class="control-label">{{ 'lbl.MaximumWidth'|trans|capitalize }}</label>
              {% form_field_error ckfinder_image_max_width %} {% form_field ckfinder_image_max_width %}
              <span class="help-block">{{ 'msg.HelpCkfinderMaximumWidth'|trans|capitalize }}</span>
            </div>
            <div class="form-group">
              <label for="ckfinderImageMaxHeight" class="control-label">{{ 'lbl.MaximumHeight'|trans|capitalize }}</label>
              {% form_field_error ckfinder_image_max_height %} {% form_field ckfinder_image_max_height %}
              <span class="help-block">{{ 'msg.HelpCkfinderMaximumHeight'|trans|capitalize }}</span>
            </div>
          </div>
        </div>
      </div>
    </div>
    <div class="row fork-module-content">
      <div class="col-md-12">
        <div class="panel panel-default">
          <div class="panel-heading">
            <h3 class="panel-title">Facebook</h3>
          </div>
          <div class="panel-body">
            <div class="form-group">
              <label for="addValue-facebookAdminIds" class="control-label">{{ 'lbl.AdminIds'|trans|capitalize }}</label>
              {% form_field_error facebook_admin_ids %} {% form_field facebook_admin_ids %}
              <span class="help-block">{{ 'msg.HelpFacebookAdminIds'|trans|capitalize }}</span>
            </div>
            <div class="form-group">
              <label for="facebookApplicationId" class="control-label">{{ 'lbl.ApplicationId'|trans|capitalize }}</label>
              {% form_field_error facebook_application_id %} {% form_field facebook_application_id %}
              <span class="help-block">{{ 'msg.HelpFacebookApplicationId'|trans|capitalize }}</span>
            </div>
            <div class="form-group">
              <label for="facebookApplicationSecret" class="control-label">{{ 'lbl.ApplicationSecret'|trans|capitalize }}</label>
              {% form_field_error facebook_application_secret %} {% form_field facebook_application_secret %}
              <span class="help-block">{{ 'msg.HelpFacebookApplicationSecret'|trans|capitalize }}</span>
            </div>
          </div>
        </div>
      </div>
    </div>
    <div class="row fork-module-content">
      <div class="col-md-12">
        <div class="panel panel-default">
          <div class="panel-heading">
            <h3 class="panel-title">Twitter</h3>
          </div>
          <div class="panel-body">
            <div class="form-group">
              <label for="twitterSiteName" class="control-label">{{ 'lbl.TwitterSiteName'|trans|capitalize }}</label>
              <div class="form-inline">
                <div class="form-group">
                  <div class="input-group">
                    <div class="input-group-addon">@</div>
                    {% form_field twitter_site_name %}
                  </div>
                  {% form_field_error twitter_site_name %}
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
    <div class="row fork-module-content">
      <div class="col-md-12">
        <div class="panel panel-default">
          <div class="panel-heading">
            <h3 class="panel-title">{{ 'lbl.Cookies'|trans|capitalize }}</h3>
          </div>
          <div class="panel-body">
            <div class="form-group">
              <p>{{ 'msg.HelpCookies'|trans|capitalize }}</p>
              <ul class="list-unstyled">
                <li class="checkbox">
                  <label for="showCookieBar" class="control-label">{% form_field show_cookie_bar %} {{ 'msg.ShowCookieBar'|trans|capitalize }}</label>
                </li>
              </ul>
            </div>
          </div>
        </div>
      </div>
    </div>

    <div class="row fork-module-actions">
      <div class="col-md-12">
        <div class="btn-toolbar">
          <div class="btn-group pull-right" role="group">
            <button id="save" type="submit" name="save" class="btn btn-primary">{{ 'lbl.Save'|trans|capitalize }}</button>
          </div>
        </div>
      </div>
    </div>

  {% endform %}
{% endblock %}
