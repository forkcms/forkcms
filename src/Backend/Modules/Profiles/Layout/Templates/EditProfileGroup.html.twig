{# This file is generated by the Spoon2Twig Converter #}

{% extends '/Layout/Templates/base.html.twig' %}

{% block actionbar %}
  <div class="row fork-module-heading">
    <div class="col-md-6">
      <h2>{{ 'lbl.EditGroup'|trans|capitalize }}</h2>
    </div>
    <div class="col-md-6">

    </div>
  </div>
{% endblock %}

{% block content %}
  {% form editProfileGroup %}
    <div class="row fork-module-content">
      <div class="col-md-12">
        <div class="form-group">
          <label for="group" class="control-label">{{ 'lbl.Group'|trans|capitalize }}</label>
          {% form_field_error group %} {% form_field group %}
        </div>
        <div class="form-group">
          <label for="expirationDate" class="control-label">{{ 'lbl.ExpiresOn'|trans|capitalize }}:</label>
          <div class="form-inline">
            <div class="form-group">
              {% form_field_error expiration_date %} {% form_field expiration_date %}
            </div>
            <div class="form-group">
              {% form_field_error expiration_time %} {% form_field expiration_time %}
            </div>
          </div>
        </div>
      </div>
    </div>
    <div class="row fork-page-actions">
      <div class="col-md-12">
        <div class="btn-toolbar">
          <div class="btn-group pull-left" role="group">
            {% if showProfilesDeleteProfileGroup %}
            <button type="button" class="btn btn-danger" data-toggle="modal" data-target="#confirmDelete">
              <span class="fa fa-trash-o"></span>
              {{ 'lbl.Delete'|trans|capitalize }}
            </button>
            {% endif %}
          </div>
          <div class="btn-group pull-right" role="group">
            <button id="saveButton" type="submit" name="edit" class="btn btn-primary">
              <span class="fa fa-check"></span>&nbsp;{{ 'lbl.Save'|trans|capitalize }}
            </button>
          </div>
        </div>
        {% if showProfilesDeleteProfileGroup %}
        <div class="modal fade" id="confirmDelete" tabindex="-1" role="dialog" aria-labelledby="{{ 'lbl.Delete'|trans|capitalize }}" aria-hidden="true">
          <div class="modal-dialog">
            <div class="modal-content">
              <div class="modal-header">
                <span class="modal-title h4">{{ 'lbl.Delete'|trans|capitalize }}</span>
              </div>
              <div class="modal-body">
                <p>{{ 'msg.ConfirmProfileGroupDelete'|trans|sprintf(profileGroup.name ) }}</p>
              </div>
              <div class="modal-footer">
                <button type="button" class="btn btn-default" data-dismiss="modal">{{ 'lbl.Cancel'|trans|capitalize }}</button>
                <a href="{{ geturl('delete_profile_group' ) }}&amp;id={{ profileGroup.id }}" class="btn btn-primary">
                  {{ 'lbl.OK'|trans|capitalize }}
                </a>
              </div>
            </div>
          </div>
        </div>
        {% endif %}
      </div>
    </div>
  {% endform %}
{% endblock %}
