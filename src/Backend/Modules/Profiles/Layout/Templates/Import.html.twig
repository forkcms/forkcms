{# This file is generated by the Spoon2Twig Converter #}

{% extends '/Layout/Templates/base.html.twig' %}

{% block actionbar %}
  <div class="btn-toolbar pull-right">
    <div class="btn-group" role="group">
      {% if showProfilesIndex %}
        {{ macro.buttonIcon(geturl('index'), 'times', 'lbl.Cancel'|trans|capitalize ) }}
      {% endif %}
      {{ macro.buttonIcon(geturl('export_template'), 'download', 'lbl.ExportTemplate'|trans|capitalize ) }}
    </div>
  </div>
{% endblock %}

{% block content %}
  {% form import %}
    <div class="row fork-module-content">
      <div class="col-md-12">
        <div class="form-group">
          <label for="group" class="control-label">{{ 'lbl.Group'|trans|capitalize }}</label>
          {% form_field_error group %} {% form_field group %}
        </div>
        <div class="form-group">
          <label for="file" class="control-label">
            {{ 'lbl.File'|trans|capitalize }}&nbsp;
            <abbr data-toggle="tooltip" title="{{ 'lbl.RequiredField'|trans|capitalize }}">*</abbr>
          </label>
          {% form_field_error file %} {% form_field file %}
        </div>
        <div class="form-group">
          <ul class="list-unstyled">
            <li class="checkbox">
              <label>{% form_field overwrite_existing %} {{ 'msg.OverwriteExisting'|trans }}</label>
            </li>
          </ul>
        </div>
      </div>
    </div>
    <div class="row fork-module-actions">
      <div class="col-md-12">
        <div class="btn-toolbar">
          <div class="btn-group pull-right" role="group">
            <button id="importButton" type="submit" name="add" class="btn btn-primary">
              <span class="fa fa-download"></span>{{ 'lbl.Import'|trans|capitalize }}
            </button>
          </div>
        </div>
      </div>
    </div>
  {% endform %}
{% endblock %}
