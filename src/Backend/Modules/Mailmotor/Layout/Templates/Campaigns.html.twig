{# This file is generated by the Spoon2Twig Converter #}

{% include "/Layout/Templates/Head.html.twig" %}
{% include "/Layout/Templates/StructureStartModule.html.twig" %}
<div class="row fork-module-heading">
  <div class="col-md-12">
    <h2>{{ 'lbl.Campaigns'|trans|capitalize }}</h2>
    <div class="btn-toolbar pull-right">
      <div class="btn-group" role="group">
        {% if showMailmotorAddCampaign %}
        <a href="{{ geturl('add_campaign') }}" class="btn btn-default" title="{{ 'lbl.AddCampaign'|trans|capitalize }}">
          <span class="glyphicon glyphicon-plus"></span>&nbsp;
          {{ 'lbl.AddCampaign'|trans|capitalize }}
        </a>
        {% endif %}
      </div>
    </div>
  </div>
</div>
<div class="row fork-module-content">
  <div class="col-md-12">
    {% if dataGrid %}
    <form 'act.on="{{  geturl('mass_campaign_action') }}" method="get" class="forkForms submitWithLink" id="campaigns">
      {{ dataGrid|raw }}
    </form>
    {% endif %}
    {% if not dataGrid %}
    <p>{{ 'msg.NoItems'|trans|capitalize }}</p>
    {% endif %}
  </div>
</div>
{% include "/Layout/Templates/StructureEndModule.html.twig" %}
{% include "/Layout/Templates/Footer.html.twig" %}
