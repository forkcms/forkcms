{# This file is generated by the Spoon2Twig Converter #}

{% extends '/Layout/Templates/base.html.twig' %}

{% block actionbar %}
  <div class="btn-toolbar pull-right">
    <div class="btn-group" role="group">
      {% if showMailmotorAddCustomField %}
        {{ macro.buttonIcon(geturl('add_custom_field' ) ~ '&group_id=' ~ group.id, 'plus', 'lbl.AddCustomField'|trans|capitalize) }}
      {% endif %}
    </div>
  </div>
{% endblock %}

{% block content %}
  <div class="row fork-module-content">
    <div class="col-md-12">
      {% if dataGrid %}
      <form 'act.on="{{  geturl('mass_custom_field_action' ) }}" method="get" class="forkForms submitWithLink" id="massCustomFieldAction">
        <input type="hidden" name="group_id" value="{{ group.id }}" />
        {{ dataGrid }}
      </form>
      {% endif %}
      {% if not dataGrid %}
      <p>{{ msg.NoItems }}</p>
      {% endif %}
    </div>
  </div>
{% endblock %}
