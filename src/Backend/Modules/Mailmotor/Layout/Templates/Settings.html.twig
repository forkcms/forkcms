{% extends 'Layout/Templates/base.html.twig' %}

{% block content %}
  {{ form_start(form) }}
    <div class="row fork-module-content">
    <div class="col-md-12">
      <div class="panel panel-default">
        <div class="panel-heading">
          <h3 class="panel-title">
          {{ 'lbl.Settings'|trans|capitalize }}
          </h3>
        </div>
        <div class="panel-body">
          {{ form_row(form.mailEngine) }}
          <div class="mail-engine-selected">
            {{ form_row(form.apiKey) }}

            <div class="panel form-group panel-default">
              <div class="panel-heading"><label>{{ 'lbl.ListId'|trans|ucfirst }}</label></div>
              <div class="panel-body">
                <div class="form-group">
                  {{ form_label(form.listId) }}
                  {{ form_widget(form.listId) }}
                  {{ form_errors(form.listId) }}
                </div>
                {% for languageListIdForm in form.languageListIds %}
                  <div class="form-group{% if loop.last %} last{% endif %}">
                    {{ form_label(languageListIdForm, languageListIdForm.vars.name|upper|tolabel) }}
                    {{ form_widget(languageListIdForm) }}
                    {{ form_errors(languageListIdForm) }}
                  </div>
                {% endfor %}
              </div>
            </div>
          </div>
          {{ form_rest(form) }}
        </div>
      </div>
    </div>
    </div>
    <div class="row fork-module-actions">
      <div class="col-md-12">
        <div class="btn-toolbar">
          <div class="btn-group pull-right" role="group">
            <button id="save" type="submit" name="save" class="btn btn-primary">{{ 'lbl.Save'|trans|capitalize }}</button>
          </div>
        </div>
      </div>
    </div>
  {{ form_end(form) }}
{% endblock %}
