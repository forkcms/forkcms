{# This file is generated by the Spoon2Twig Converter #}

<div id="widgetMailmotorClassic" class="panel panel-primary">
  <div class="panel-heading">
    <h2 class="panel-title">
      <a href="{{ geturl('index','mailmotor') }}">{{ 'lbl.Mailmotor'|trans|capitalize }}</a>
    </h2>
  </div>
  <div class="panel-body">
    <div class="fork-tabs" role="tabpanel">
      <ul class="nav nav-tabs" role="tablist">
        <li role="presentation" class="active">
          <a href="#tabMailmotorSubscriptions" aria-controls="home" role="tab" data-toggle="tab">{{ 'lbl.Subscriptions'|trans|capitalize }}</a>
        </li>
        <li role="presentation">
          <a href="#tabMailmotorUnsubscriptions" aria-controls="profile" role="tab" data-toggle="tab">{{ 'lbl.Unsubscriptions'|trans|capitalize }}</a>
        </li>
        <li role="presentation">
          <a href="#tabMailmotorStatistics" aria-controls="messages" role="tab" data-toggle="tab">{{ 'lbl.Statistics'|trans|capitalize }}</a>
        </li>
      </ul>
      <div class="tab-content">
        <div role="tabpanel" class="tab-pane active" id="tabMailmotorSubscriptions">
          <div id="dataGridSubscriptions">
            {% if dgMailmotorSubscriptions %}
            <div class="table table-striped dataGridHolder">
              {{ dgMailmotorSubscriptions|raw }}
            </div>
            {% endif %}
            {% if not dgMailmotorSubscriptions %}
            <p>
              {{ 'msg.NoSubscriptions'|trans|capitalize }}
            </p>
            {% endif %}
          </div>
        </div>
        <div role="tabpanel" class="tab-pane" id="tabMailmotorUnsubscriptions">
          <div id="dataGridUnsubscriptions">
            {% if dgMailmotorUnsubscriptions %}
            <div class="table table-striped dataGridHolder" >
              {{ dgMailmotorUnsubscriptions|raw }}
            </div>
            {% endif %}
            {% if not dgMailmotorUnsubscriptions %}
            <p>
              {{ 'msg.NoUnsubscriptions'|trans|capitalize }}
            </p>
            {% endif %}
          </div>
        </div>
        <div role="tabpanel" class="tab-pane" id="tabMailmotorStatistics">
          <div id="dataGridStatistics">
            {% if dgMailmotorStatistics %}
            <div class="table table-striped dataGridHolder">
              {{ dgMailmotorStatistics|raw }}
            </div>
            {% endif %}
            {% if not dgMailmotorStatistics %}
            <p>
              {{ 'msg.NoSentMailings'|trans|capitalize }}
            </p>
            {% endif %}
          </div>
        </div>
      </div>
    </div>
  </div>
  <div class="panel-footer">
    <div class="btn-toolbar">
      <div class="btn-group">
        <a href="{{ geturl('addresses','mailmotor') }}" class="btn"><span>{{ 'msg.AllAddresses'|trans|capitalize }}</span></a>
      </div>
    </div>
  </div>
</div>
