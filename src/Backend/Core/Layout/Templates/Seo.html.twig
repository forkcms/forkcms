{# This file is generated by the Spoon2Twig Converter #}

<div class="row">
  <div class="col-md-12">
    <h3>{{ 'lbl.SEO'|trans|capitalize }}</h3>
  </div>
</div>
<div class="row">
  <div class="col-md-12">
    <h4>{{ 'lbl.Titles'|trans|capitalize }}</h4>
    <div class="form-group">
      <ul class="list-unstyled checkboxTextFieldCombo">
        <li class="checkbox">
          <label for="pageTitleOverwrite" class="visuallyHidden">{% form_field page_title_overwrite %} <b>{{ 'lbl.PageTitle'|trans|capitalize }}</b></label>
          <p class="text-info">{{ 'msg.HelpPageTitle'|trans|capitalize }}</p>
          {% form_field_error page_title %} {% form_field page_title %}
        </li>
      </ul>
    </div>
    {% if chkNavigationTitleOverwrite %}
    <div class="form-group">
      <ul class="list-unstyled checkboxTextFieldCombo">
        <li class="checkbox">
          <label for="navigationTitleOverwrite" class="visuallyHidden">{% form_field navigation_title_overwrite %} <b>{{ 'lbl.NavigationTitle'|trans|capitalize }}</b></label>
          <p class="text-info">{{ 'msg.HelpNavigationTitle'|trans|capitalize }}</p>
          {% form_field_error navigation_title %} {% form_field navigation_title %}
        </li>
      </ul>
    </div>
    {% endif %}
  </div>
</div>
<div class="row">
  <div class="col-md-12">
    <h4>{{ 'lbl.MetaInformation'|trans|capitalize }}</h4>
    <div class="form-group">
      <ul class="list-unstyled checkboxTextFieldCombo">
        <li class="checkbox">
          <label for="metaDescriptionOverwrite" class="visuallyHidden">{% form_field meta_description_overwrite %} <b>{{ 'lbl.Description'|trans|capitalize }}</b></label>
          <p class="text-info">{{ 'msg.HelpMetaDescription'|trans|capitalize }}</p>
          {% form_field_error meta_description %} {% form_field meta_description %}
        </li>
      </ul>
    </div>
    <div class="form-group">
      <ul class="list-unstyled checkboxTextFieldCombo">
        <li class="checkbox">
          <label for="metaDescriptionOverwrite" class="visuallyHidden">{% form_field meta_keywords_overwrite %} <b>{{ 'lbl.Keywords'|trans|capitalize }}</b></label>
          <p class="text-info">{{ 'msg.HelpMetaKeywords'|trans|capitalize }}</p>
          {% form_field_error meta_keywords %} {% form_field meta_keywords %}
        </li>
      </ul>
    </div>
    <div class="form-group">
      <label for="metaDescriptionOverwrite" class="visuallyHidden">{{ 'lbl.ExtraMetaTags'|trans|capitalize }}</label>
      <p class="text-info">{{ 'msg.HelpMetaCustom'|trans|capitalize }}</p>
      {% form_field_error meta_custom %} {% form_field meta_custom %}
    </div>
  </div>
</div>
<div class="row">
  <div class="col-md-12">
    <h4>{{ 'lbl.URL'|trans|capitalize }}</h4>
    <div class="form-group">
      <ul class="list-unstyled checkboxTextFieldCombo">
        <li class="checkbox">
          <label for="urlOverwrite" class="visuallyHidden">{% form_field url_overwrite %} <b>{{ 'lbl.CustomURL'|trans|capitalize }}</b></label>
          <p class="text-info">{{ 'msg.HelpMetaURL'|trans|capitalize }}</p>
          <div class="form-inline">
            <span id="urlFirstPart">
              {% if detailUrl %}{{ detailUrl }}{% endif %}
              {% if not detailUrl %}{{ SITE_URL }}{% if prefixURL %}{{ prefixURL }}{% endif %}/{% endif %}
            </span>
            {% form_field_error url %} {% form_field url %}
          </div>
        </li>
      </ul>
    </div>
  </div>
</div>
<div class="row">
  <div class="col-md-12">
    <h4>{{ 'lbl.SEO'|trans|capitalize }}</h4>
    <div class="form-inline">
      <div class="form-group">
        <p><b>{{ 'lbl.Index'|trans|capitalize }}</b></p>
        {% if rbtSeoIndexError %}
        <div class="alert alert-danger">{{ rbtSeoIndexError }}</div>
        {% endif %}
        <ul class="list-unstyled inputListHorizontal">
          {% for index in seo_index %}
          <li class="radio">
            <label for="{{ index.id }}">{{ index.rbtSeoIndex|raw }} {{ index.label }}</label>
          </li>
          {% endfor %}
        </ul>
      </div>
    </div>
    <div class="form-inline">
      <div class="form-group">
        <p><b>{{ 'lbl.Follow'|trans|capitalize }}</b></p>
        {% if rbtSeoFollowError %}
        <div class="alert alert-danger">{{ rbtSeoFollowError }}</div>
        {% endif %}
        <ul class="list-unstyled inputListHorizontal">
          {% for follow in seo_follow %}
          <li class="radio">
            <label for="{{ follow.id }}">{{ follow.rbtSeoFollow|raw }} {{ follow.label }}</label>
          </li>
          {% endfor %}
        </ul>
      </div>
    </div>
  </div>
</div>
{# Hidden settings, used for the Ajax-call to verify the url #}
{{ hidMetaId|raw }}
{{ hidBaseFieldName|raw }}
{{ hidCustom|raw }}
{{ hidClassName|raw }}
{{ hidMethodName|raw }}
{{ hidParameters|raw }}
