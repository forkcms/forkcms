imports:
    - { resource: parameters.yml }

framework:
    secret:          %kernel.secret%
    router:
        resource: "%kernel.root_dir%/config/routing.yml"
        strict_requirements: ~
    form:            ~
    csrf_protection: ~
    templating:
        engines: ['twig']
    session:         ~

# Assetic Configuration
assetic:
    debug:          "%kernel.debug%"
    use_controller: false
    bundles:        [ 'ForkCMSInstallerBundle' ]
    filters:
        cssrewrite: ~

# Doctrine Configuration
doctrine:
    dbal:
        # this is a little bit nasty, but we need pdo_ for doctrine and spoonDatabase to work together
        driver:   "pdo_%database.driver%"
        host:     "%database.host%"
        port:     "%database.port%"
        dbname:   "%database.name%"
        user:     "%database.user%"
        password: "%database.password%"
        charset:  UTF8

    orm:
        auto_generate_proxy_classes: "%kernel.debug%"
        auto_mapping: true
        mappings:
            ContentBlocks:
                type: annotation
                dir: %kernel.root_dir%/../src/Backend/Modules/ContentBlocks/Entity
                prefix: Backend\Modules\ContentBlocks\Entity
            Location:
                type: annotation
                dir: %kernel.root_dir%/../src/Backend/Modules/Location/Entity
                prefix: Backend\Modules\Location\Entity

twig:
    debug:            "%kernel.debug%"
    strict_variables: "%kernel.debug%"


monolog:
    handlers:
        main:
            type:  stream
            path:  %site.path_www%/app/logs/logs.log
            level: debug

services:
    database:
        class: SpoonDatabase
        arguments:
            - %database.driver%
            - %database.host%
            - %database.user%
            - %database.password%
            - %database.name%
            - %database.port%
        calls:
            - [ execute, [ 'SET CHARACTER SET :charset, NAMES :charset, time_zone = "+0:00"', { 'charset': 'utf8' } ] ]
            - [ setDebug, [ %kernel.debug% ]]
    mailer:
        class: Common\Mailer
        arguments:
            - @database
