language: php

php:
  - 5.3
  - 5.4
  - 5.5
  - 5.6
  - hhvm
  
matrix:
  allow_failures:
    - php: hhvm

script:
  - build/./run-tests

before_script:
  - composer self-update || true
  - composer update -o --prefer-source
